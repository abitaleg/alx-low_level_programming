#include <unistd.h>

/**
 * _putchar -> writes a charcter c to stdou
 * @c:The chareter to print out
 * Return: on sucess 1.
 * on error, -1 is returned, and errno is set approxmately.
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}
