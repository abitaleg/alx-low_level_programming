#include "main.h"

/**
 *_islower-> it checks if the char is lowercase or not.
 *@c: is the character.
 * Return: returns 1 or 0 depends on the character.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
